# docker swarm

## Create cluster

standard quorum rules apply (splitbrain etc), so follow normal 3 = good 2 = bad. 

## Node Management

Collection of node commands

### node status

``` c#
docker system info
```

### join cluster

``` c#
docker swarm leave
```

### leave cluster

``` c#
docker swarm leave
```
